class controller {
    constructor() {
        this.initialize();
    }

    async initialize() {
        //const { default: autoBind } = await import('auto-bind');
        //autoBind(this);
    }
}

export default controller;
module.exports = controller;